
class Solution {
    public void reverseArray(int arr[]) {
        int a=arr.length-1;
        int b=0;
        
        while(b<a){
            int temp=arr[b];
            arr[b]=arr[a];
            arr[a]=temp;
             b++;
            a--;
           
        }
    }
}
